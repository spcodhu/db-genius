###############################################################################
# Environment for the full-stack compose (deploy/standalone/docker-compose.yml)
# Copy to .env in the same directory and edit the values.
###############################################################################

# ===== PostgreSQL container (created by this compose) =====
POSTGRES_DB=db_genius
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
# Host port mapping for the postgres container
POSTGRES_PORT=5432

# ===== Server =====
SERVER_PORT=8109

# ===== AI model fallback (used when a user has no personal model config) =====
DB_GENIUS_DEFAULT_MODEL_API_KEY=sk-your-api-key
DB_GENIUS_DEFAULT_MODEL_BASE_URL=https://api.deepseek.com
DB_GENIUS_DEFAULT_MODEL=deepseek-v4-pro

# ===== AES-256-GCM key for database credentials (exactly 32 characters) =====
DB_GENIUS_ENCRYPT_KEY=your-32-character-secret-key!!!!

# ===== Aliyun OSS (file upload storage, required) =====
ALIYUN_OSS_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com
ALIYUN_OSS_BUCKET=your-bucket-name
ALIYUN_ACCESS_KEY_ID=your-access-key-id
ALIYUN_ACCESS_KEY_SECRET=your-access-key-secret
# Object key prefix (optional)
ALIYUN_OSS_DIR_PREFIX=uploads/

# ===== Aliyun OCR (optional, default disabled) =====
ALIYUN_OCR_ENABLED=false
ALIYUN_OCR_ENDPOINT=ocr-api.cn-hangzhou.aliyuncs.com

# ===== RabbitMQ (container created by this compose) =====
SPRING_RABBITMQ_USERNAME=guest
SPRING_RABBITMQ_PASSWORD=guest
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672

# ===== Trial mode (public demo) =====
DB_GENIUS_TRIAL_ENABLED=false
