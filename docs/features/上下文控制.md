
1. 当前采用的模型是 deepseek-v4-pro，理论可使用的上下文窗口是 1M (单次对话)。实际智能体的上下文窗口还需要再计算；
2. 应该在每次工具调用或者是每次会话输出之后计算本地调用/会话消耗的 token（包括输入和输出的），超出一定阈值就启动压缩（类似于 Claude Code 的 /compact 命令）；
3. 由于本智能体是操作数据库的，天然存在 Tools 超长输出风险（比如一次查数据库返回了几百万行数据），应该在 Tools 里面定义时告诉 AI 如何分页；其余的 Tools 也要排查，按照如此设计；

